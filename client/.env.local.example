# Usage counter endpoint toggle (prevents 404s in dev if the route isn't implemented)
VITE_USAGE_ENABLED=false

# If your backend implements GET /api/usage, set this to true:
# VITE_USAGE_ENABLED=true

# Optional: your API base (defaults to http://localhost:3000)
# VITE_API_BASE_URL=http://localhost:3000
